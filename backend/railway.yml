version: '1.0'
services:
  backend:
    commands:
      - pip install -r requirements.txt  # Install dependencies
      - npx prisma generate
      - prisma run
      - python main.py  # Start your Flask application

